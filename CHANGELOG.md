# 更新日志

本项目使用更新日志记录仓库文档、应用功能和维护信息的主要变化。

当前仓库尚未声明正式发布版本号，也未提供 GitHub Release 正式安装包；以下内容用于记录当前 `new-something` 分支已经具备的实际能力，不虚构发布日期或正式版本。

## Unreleased

### Added

- 新增 GitHub 仓库首页文档，统一项目对外展示名称为“无广告极简记账 App”，英文辅助名称为 `Minimal Ad-Free Expense Tracker`。
- 新增项目简介、设计目标、主要功能、应用页面、数据与隐私、导入导出、使用说明、开发者说明和维护说明。
- 新增完整 JSON 备份说明，明确完整备份包含 `records` 和 `categories`。
- 新增数据导入兼容说明，覆盖旧账单列表 JSON、独立分类列表 JSON、完整备份 JSON、CSV 和 XLSX。
- 新增 GitHub Actions 手动构建 debug APK 的文档说明。
- 新增参与贡献说明，包含 Issue、Bug 报告、Pull Request、基础检查、Android 实机测试和敏感文件提交限制。
- 新增更完整的 `.gitignore` 规则，覆盖 Python 缓存、虚拟环境、Buildozer 构建产物、APK/AAB、签名文件、IDE 文件、本地账单数据、导入导出临时文件和用户导出的账单备份文件。

### Changed

- 将仓库文档整理为以中文为主，兼顾普通用户、作者维护和开发者构建的说明结构。
- 明确“无广告”仅表示当前项目代码不包含广告功能或第三方广告 SDK，不对未来版本或第三方修改版作保证。
- 明确当前项目为本地离线应用，不包含账号系统、登录、云同步、远程服务器账单存储或应用商店正式发布说明。
- 明确 Excel 和 CSV 导出只包含账单记录，不包含自定义分类配置；完整 JSON 备份适合迁移和恢复应用数据。
- 明确 debug APK 可通过 GitHub Actions 手动构建，debug APK 不等同于正式发布版本。

### Fixed

- 修正仓库说明缺失或过时导致的误解风险，避免让用户误以为项目包含云同步、账号系统、广告能力、正式 Release 安装包或应用商店发布。
- 补充卸载应用前应导出完整 JSON 备份的提醒，降低用户误删本地数据的风险。
- 补充不要提交真实账单、导出备份、密钥和签名文件的维护提醒。
