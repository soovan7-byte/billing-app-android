# 更新日志

本文件用于记录“无广告极简记账 App”的主要功能变化。

## [1.0.1] - 2026-08-29

### Changed

- Android 冷启动优化：openpyxl（Excel 导入导出）改为按需导入，不再占用启动关键路径；
- 支出/收入/统计/设置页面改为首次进入时按需构建（lazy construction），启动只构建首屏；
- 主页面切换动画提速（0.4s → 0.2s）；
- 底部导航改为共享固定组件，切换页面时底栏保持静止，仅内容区参与滑动过渡；
- 收入功能开关即时更新共享底栏（记账/支出/收入/统计），设置页行为保持不变。

### Fixed

- 直接刷新尚未进入的页面时自动构建其骨架（_ensure_screen），避免空引用；
- 快速连续切换页面时动画自动取消，最终选中页与高亮保持一致。

## [1.0.0] - 2026-08-29

### Added

- 记账、明细、统计、设置四页面移动端结构；
- 固定底部导航；
- 快速记账流程；
- 紧凑日期选择；
- 非阻断记账成功提示；
- 明细记录详情；
- 单条记录删除与二次确认；
- 月度统计；
- 年度统计；
- 分类饼图和分类消费占比；
- 分类管理即时刷新；
- 分类删除确认；
- 设置页数据概览；
- Excel、CSV 和 XLSX 数据处理；
- 包含 records 和 categories 的完整 JSON 备份；
- Android SAF 系统文件选择器导入导出；
- 分类管理支持上移、下移调整顺序；
- 支持点击统计饼图分类区域查看当前周期对应消费记录；
- 统计页支持查看当前周期单笔消费金额前20排名；
- 可选收入功能（默认关闭，可在设置页开启）；
- 独立收入页面与收入分类管理；
- 统计页收支概览卡（收入、支出、收支差）；
- 记录 ID 幂等去重，导入历史备份不产生重复记录；
- 支出、收入、分类详情、单笔消费排名统一使用同一记录卡片布局；
- 分类设置项按分类整行浅色背景分组。

### Changed

- 项目对外展示名称统一为“无广告极简记账 App”；
- 重新设计移动端视觉主题；
- 表单顺序调整为金额、分类、备注、日期；
- 查看记录和单条删除整合到明细页面；
- 月度与年度统计从 Popup 整合到统计页面；
- JSON 导出从单纯账单列表升级为账单和分类完整备份；
- 设置页重新整理分类管理、数据管理和危险操作入口；
- Excel 与 CSV 在无有效记录时统一停止导出空表格。
- 用户可见的“明细”页面更名为“支出”；
- 底部页面切换动画会根据页面顺序自动选择左右方向；
- 记账、支出和统计页面均可直接进入设置。

### Fixed

- Android 输入框文字不可见；
- 删除记录后列表不即时刷新；
- 连续删除可能使用过期索引并删除错误记录；
- 分类列表新增或删除后不能即时刷新；
- 分类弹窗关闭后保留失效控件引用；
- 确认按钮快速连点可能重复执行；
- 成功反馈定时回调可能在界面销毁后继续执行；
- 非字典异常历史数据可能导致明细页崩溃；
- 异常金额可能导致旧记录窗口崩溃；
- 异常数据可能导致月度汇总、统计或表格导出失败；
- 本地 records.json 或 categories.json 类型错误时可能引发异常；
- 桌面与 Android 的 Excel、CSV 空记录导出行为不一致；
- 修复自定义分类排序在重新启动后不能可靠保持的问题；
- 修复删除默认分类后重新启动可能再次出现的问题；
- 页面切换与列表刷新性能优化（dirty 标记缓存）；
- 记录卡片“日期 · 分类”左对齐、备注按实际宽度完整显示；
- 分类设置项名称左上对齐；
- Android 收入开关滑块渲染在轨道之外的问题（轨道与滑块统一画布坐标空间重绘）；
- 分类名称超长时字号自适应与两行显示；
- 收入/支出/收支差概览与年度月度统计口径分离。
